server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/identity_service"
    username: root
    password: 12345678
  # cho phep tu tao bang
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true